# overwatch
Overwatch is an independent atmospheric audit layer that periodically pulls the system state, analyzes it, and stores the results in its own, isolated archive. ROOT cannot modify it, and Overwatch cannot write to ROOT.
